# 1단계 — 손 입력 예약 앱 구현 계획

> **작업자에게:** 이 계획은 작업 단위로 하나씩 실행한다. 각 단계는 체크박스(`- [ ]`)로 추적한다.

**목표:** 서버·의존성·비용 없이 폰에서 돌아가는 예약 관리 웹앱을 완성한다. 예약을 직접 입력·취소하고, 손님 이름을 누르면 방문 이력이 나온다.

**아키텍처:** 정적 파일만으로 구성된 다중 페이지 웹앱(PWA). 화면 3개(홈 / 손님 이력 / 설정)를 각각 독립된 HTML 파일로 만들고, 데이터는 브라우저 `localStorage`에 JSON 한 덩어리로 저장한다. 로직(저장소·통계·시술 목록)은 DOM과 분리된 순수 함수로 두어 Node에서 테스트한다.

**기술 스택:** 순수 HTML / CSS / JavaScript (ES 모듈), `localStorage`, `node --test`. **외부 라이브러리 0개.**

## 전역 제약

이 계획의 모든 작업에 공통으로 적용된다.

- **의존성 0.** `package.json`의 `dependencies`와 `devDependencies`는 끝까지 비어 있어야 한다. 빌드 도구·번들러·프레임워크 없음.
- **서버 없음.** 백엔드, API 호출, 네트워크 요청이 하나도 없다. 1단계는 완전히 오프라인으로 동작한다.
- **비용 0원.** 유료 서비스에 의존하는 코드를 넣지 않는다.
- **주석은 한국어, 변수명·함수명은 영어.**
- **ES 모듈만 사용.** `package.json`에 `"type": "module"`. `require()` 금지, `import`/`export`만.
- **로직과 DOM 분리.** `js/services.js`, `js/store.js`, `js/stats.js`는 `document`, `window`, `localStorage`를 직접 참조하지 않는다. 저장소는 주입받는다. 이래야 Node에서 테스트할 수 있다.
- **날짜 형식은 `YYYY-MM-DD` 문자열, 시각은 `HH:MM` 문자열.** `Date` 객체를 저장하지 않는다(직렬화 문제와 시간대 함정을 피한다).
- **1단계 범위 밖:** AI 사진 판독, 알림, 서버 동기화. 이것들을 위한 코드를 미리 넣지 않는다.

---

## 파일 구조

```
reservation/
├── package.json          테스트 실행 설정 (의존성 없음)
├── index.html            홈 — 예약 목록 + 추가 폼
├── customer.html         손님 이력
├── settings.html         설정 — 데이터 내보내기
├── style.css             전체 스타일
├── manifest.json         PWA 설치 정보
├── sw.js                 서비스 워커 (오프라인 동작)
├── icon-192.png          앱 아이콘
├── icon-512.png          앱 아이콘
├── js/
│   ├── services.js       시술 목록과 기본 소요 시간 (순수 데이터)
│   ├── store.js          예약 저장·조회·취소 (저장소 주입)
│   ├── stats.js          손님 방문 이력 계산 (순수 함수)
│   ├── dates.js          날짜 표시 형식 변환 (홈·이력 화면 공용)
│   ├── home.js           홈 화면 동작
│   ├── customer.js       손님 이력 화면 동작
│   └── settings.js       설정 화면 동작
└── test/
    ├── services.test.js
    ├── store.test.js
    └── stats.test.js
```

**책임 분리 원칙:** `js/` 안의 앞 세 파일(`services`, `store`, `stats`)은 순수 로직이고 테스트 대상이다. 뒤 세 파일(`home`, `customer`, `settings`)은 화면당 하나씩 대응하며 DOM만 다룬다. 화면 파일에 계산 로직을 쓰지 않는다.

---

## Task 1: 프로젝트 뼈대와 시술 목록

**파일:**
- 생성: `package.json`
- 생성: `js/services.js`
- 생성: `test/services.test.js`
- 생성: `.gitignore`

**인터페이스:**
- 제공: `SERVICES` (배열), `defaultMinutes(name)` → `number`

---

- [ ] **1-1. `.gitignore` 만들기**

```
.DS_Store
node_modules/
```

- [ ] **1-2. `package.json` 만들기**

```json
{
  "name": "reservation",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test"
  }
}
```

`"type": "module"`이 있어야 테스트 파일에서 `import`를 쓸 수 있다. 의존성은 하나도 없다.

- [ ] **1-3. 실패하는 테스트 작성**

`test/services.test.js`:

```js
import { test } from 'node:test'
import assert from 'node:assert/strict'
import { SERVICES, defaultMinutes } from '../js/services.js'

test('시술 목록은 7개다', () => {
  assert.equal(SERVICES.length, 7)
})

test('시술 목록의 첫 항목은 남자커트다', () => {
  // 언니가 많이 하는 순서대로 정렬되어 있어야 입력할 때 위쪽에서 바로 고른다
  assert.equal(SERVICES[0].name, '남자커트')
})

test('확인된 기본 소요 시간을 돌려준다', () => {
  assert.equal(defaultMinutes('남자커트'), 30)
  assert.equal(defaultMinutes('펌'), 150)
})

test('목록에 없는 시술은 30분으로 처리한다', () => {
  assert.equal(defaultMinutes('없는시술'), 30)
})
```

- [ ] **1-4. 테스트를 실행해 실패를 확인**

실행: `npm test`
예상: `Cannot find module '../js/services.js'` 로 실패

- [ ] **1-5. `js/services.js` 구현**

```js
// 시술 목록. 언니가 많이 하는 순서대로 둔다 — 입력할 때 위에서 바로 고를 수 있게.
// defaultMin은 기본 소요 시간(분). 앱에서 10분 단위로 조절 가능하므로
// 틀려도 치명적이지 않지만, 실제와 가까울수록 손대는 횟수가 줄어든다.
export const SERVICES = [
  { name: '남자커트', defaultMin: 30 },  // 확인됨
  { name: '펌', defaultMin: 150 },       // 확인됨 (120~180분)
  { name: '여자커트', defaultMin: 40 },  // 임시값 — 언니 확인 필요
  { name: '염색', defaultMin: 90 },      // 임시값
  { name: '매직', defaultMin: 150 },     // 임시값
  { name: '클리닉', defaultMin: 40 },    // 임시값
  { name: '기타', defaultMin: 30 },
]

// 시술 이름으로 기본 소요 시간을 찾는다. 모르는 이름은 30분으로 둔다.
export function defaultMinutes(name) {
  const found = SERVICES.find((s) => s.name === name)
  return found ? found.defaultMin : 30
}
```

- [ ] **1-6. 테스트를 실행해 통과를 확인**

실행: `npm test`
예상: 4개 테스트 모두 PASS

- [ ] **1-7. 커밋**

```bash
git add .gitignore package.json js/services.js test/services.test.js
git commit -m "시술 목록과 기본 소요 시간 추가"
```

---

## Task 2: 예약 저장소

**파일:**
- 생성: `js/store.js`
- 생성: `test/store.test.js`

**인터페이스:**
- 사용: `defaultMinutes` (Task 1)
- 제공: `createStore(storage, deps)` → `{ list, add, cancel, remove, byDate, byCustomer, exportJson }`

**설계 메모:** `localStorage`를 직접 부르지 않고 **주입받는다.** 브라우저에서는 진짜 `localStorage`를, 테스트에서는 가짜 객체를 넣는다. glean에서 배운 의존성 주입과 같은 방식이다. `crypto.randomUUID`와 현재 시각도 같은 이유로 주입한다 — 그래야 테스트 결과가 매번 같다.

---

- [ ] **2-1. 실패하는 테스트 작성**

`test/store.test.js`:

```js
import { test } from 'node:test'
import assert from 'node:assert/strict'
import { createStore } from '../js/store.js'

// localStorage 흉내. 브라우저 없이 테스트하기 위한 가짜 저장소.
function fakeStorage() {
  const data = new Map()
  return {
    getItem: (k) => (data.has(k) ? data.get(k) : null),
    setItem: (k, v) => data.set(k, v),
  }
}

// id와 시각을 고정해 테스트 결과가 매번 같게 만든다
function makeStore() {
  let n = 0
  return createStore(fakeStorage(), {
    uid: () => `id${++n}`,
    now: () => '2026-03-17T09:00:00.000Z',
  })
}

test('처음에는 예약이 없다', () => {
  const store = makeStore()
  assert.deepEqual(store.list(), [])
})

test('예약을 추가하면 목록에 들어간다', () => {
  const store = makeStore()
  store.add({ date: '2026-03-17', time: '10:20', customerName: '화선언니', service: '염색' })

  const rows = store.list()
  assert.equal(rows.length, 1)
  assert.equal(rows[0].customerName, '화선언니')
  assert.equal(rows[0].service, '염색')
})

test('추가할 때 빠진 값은 자동으로 채워진다', () => {
  const store = makeStore()
  store.add({ date: '2026-03-17', time: '10:20', service: '염색' })

  const r = store.list()[0]
  assert.equal(r.id, 'id1')
  assert.equal(r.customerName, '')     // 이름은 없을 수 있다
  assert.equal(r.durationMin, 90)      // 염색 기본값
  assert.equal(r.status, 'active')
  assert.equal(r.source, 'manual')
  assert.equal(r.createdAt, '2026-03-17T09:00:00.000Z')
  assert.equal(r.updatedAt, '2026-03-17T09:00:00.000Z')
})

test('소요 시간을 직접 주면 기본값 대신 그 값을 쓴다', () => {
  const store = makeStore()
  store.add({ date: '2026-03-17', time: '10:20', service: '염색', durationMin: 120 })
  assert.equal(store.list()[0].durationMin, 120)
})

test('취소하면 지워지지 않고 상태만 바뀐다', () => {
  const store = makeStore()
  store.add({ date: '2026-03-17', time: '10:20', service: '염색' })
  store.cancel('id1')

  const rows = store.list()
  assert.equal(rows.length, 1)          // 여전히 남아 있다
  assert.equal(rows[0].status, 'cancelled')
})

test('삭제하면 목록에서 사라진다', () => {
  const store = makeStore()
  store.add({ date: '2026-03-17', time: '10:20', service: '염색' })
  store.remove('id1')
  assert.equal(store.list().length, 0)
})

test('날짜로 찾으면 시간순으로 나온다', () => {
  const store = makeStore()
  store.add({ date: '2026-03-17', time: '18:00', service: '펌' })
  store.add({ date: '2026-03-17', time: '10:20', service: '염색' })
  store.add({ date: '2026-03-18', time: '11:00', service: '매직' })

  const rows = store.byDate('2026-03-17')
  assert.equal(rows.length, 2)
  assert.equal(rows[0].time, '10:20')
  assert.equal(rows[1].time, '18:00')
})

test('손님 이름으로 찾으면 최근 방문이 먼저 나온다', () => {
  const store = makeStore()
  store.add({ date: '2026-01-20', time: '10:00', customerName: '화선언니', service: '펌' })
  store.add({ date: '2026-03-17', time: '10:20', customerName: '화선언니', service: '염색' })
  store.add({ date: '2026-03-18', time: '11:00', customerName: '김미영', service: '매직' })

  const rows = store.byCustomer('화선언니')
  assert.equal(rows.length, 2)
  assert.equal(rows[0].date, '2026-03-17')
  assert.equal(rows[1].date, '2026-01-20')
})

test('취소된 예약은 손님 이력에서 빠진다', () => {
  const store = makeStore()
  store.add({ date: '2026-03-17', time: '10:20', customerName: '화선언니', service: '염색' })
  store.cancel('id1')
  assert.equal(store.byCustomer('화선언니').length, 0)
})

test('내보내기는 예약 전체를 JSON 문자열로 준다', () => {
  const store = makeStore()
  store.add({ date: '2026-03-17', time: '10:20', service: '염색' })

  const parsed = JSON.parse(store.exportJson())
  assert.equal(parsed.length, 1)
  assert.equal(parsed[0].service, '염색')
})
```

- [ ] **2-2. 테스트를 실행해 실패를 확인**

실행: `npm test`
예상: `Cannot find module '../js/store.js'` 로 실패

- [ ] **2-3. `js/store.js` 구현**

```js
import { defaultMinutes } from './services.js'

const KEY = 'reservations'

// 저장소를 만든다.
// storage: localStorage와 같은 모양의 객체 (getItem / setItem)
// deps: 테스트에서 결과를 고정하기 위해 id 생성기와 시계를 갈아끼울 수 있게 열어둔다
export function createStore(storage, deps = {}) {
  const uid = deps.uid || (() => crypto.randomUUID())
  const now = deps.now || (() => new Date().toISOString())

  const load = () => JSON.parse(storage.getItem(KEY) || '[]')
  const save = (rows) => storage.setItem(KEY, JSON.stringify(rows))

  return {
    // 전체 예약 (취소된 것 포함)
    list() {
      return load()
    },

    // 예약 추가. 빠진 값은 기본값으로 채운다.
    // id와 updatedAt은 지금 쓰지 않지만, 나중에 서버를 붙일 때 없으면
    // 데이터를 갈아엎어야 하므로 미리 넣어둔다.
    add(input) {
      const rows = load()
      const stamp = now()
      const row = {
        id: uid(),
        date: input.date,
        time: input.time,
        customerName: input.customerName || '',
        service: input.service,
        durationMin: input.durationMin ?? defaultMinutes(input.service),
        status: 'active',
        source: input.source || 'manual',
        createdAt: stamp,
        updatedAt: stamp,
      }
      rows.push(row)
      save(rows)
      return row
    },

    // 취소. 지우지 않고 상태만 바꾼다 —
    // 이 손님이 지난달에도 취소했는지 알 수 있어야 하기 때문.
    cancel(id) {
      const rows = load()
      const row = rows.find((r) => r.id === id)
      if (!row) return
      row.status = 'cancelled'
      row.updatedAt = now()
      save(rows)
    },

    // 완전 삭제. 잘못 넣은 예약을 지울 때만 쓴다.
    remove(id) {
      save(load().filter((r) => r.id !== id))
    },

    // 특정 날짜의 예약을 시간순으로
    byDate(date) {
      return load()
        .filter((r) => r.date === date)
        .sort((a, b) => a.time.localeCompare(b.time))
    },

    // 특정 손님의 방문 기록을 최근순으로 (취소된 것 제외)
    byCustomer(name) {
      return load()
        .filter((r) => r.customerName === name && r.status === 'active')
        .sort((a, b) => b.date.localeCompare(a.date))
    },

    // 백업용 내보내기
    exportJson() {
      return JSON.stringify(load(), null, 2)
    },
  }
}
```

- [ ] **2-4. 테스트를 실행해 통과를 확인**

실행: `npm test`
예상: 14개 테스트 모두 PASS (Task 1의 4개 + 이번 10개)

- [ ] **2-5. 커밋**

```bash
git add js/store.js test/store.test.js
git commit -m "예약 저장소 추가 — 추가·취소·날짜별·손님별 조회"
```

---

## Task 3: 손님 방문 이력 계산

**파일:**
- 생성: `js/stats.js`
- 생성: `test/stats.test.js`

**인터페이스:**
- 제공: `customerStats(visits)` → `{ count, lastVisit, avgIntervalDays }`

**설계 메모:** 이 파일은 저장소를 모른다. 예약 배열을 받아 계산만 한다. Python으로 하던 정렬·필터·집계와 같은 일이므로 직접 작성해볼 만한 부분이다.

---

- [ ] **3-1. 실패하는 테스트 작성**

`test/stats.test.js`:

```js
import { test } from 'node:test'
import assert from 'node:assert/strict'
import { customerStats } from '../js/stats.js'

test('방문 기록이 없으면 0회', () => {
  const s = customerStats([])
  assert.equal(s.count, 0)
  assert.equal(s.lastVisit, null)
  assert.equal(s.avgIntervalDays, null)
})

test('한 번만 방문했으면 평균 간격은 알 수 없다', () => {
  const s = customerStats([{ date: '2026-03-17' }])
  assert.equal(s.count, 1)
  assert.equal(s.lastVisit, '2026-03-17')
  assert.equal(s.avgIntervalDays, null)  // 간격을 잴 두 점이 없다
})

test('두 번 방문하면 그 사이 일수가 평균 간격', () => {
  // 1/20 → 3/17 은 56일
  const s = customerStats([{ date: '2026-03-17' }, { date: '2026-01-20' }])
  assert.equal(s.count, 2)
  assert.equal(s.avgIntervalDays, 56)
})

test('세 번 이상이면 처음부터 마지막까지를 간격 수로 나눈다', () => {
  // 1/1 → 1/11 → 1/21 : 총 20일을 2번의 간격으로 나눠 10일
  const s = customerStats([
    { date: '2026-01-21' },
    { date: '2026-01-11' },
    { date: '2026-01-01' },
  ])
  assert.equal(s.count, 3)
  assert.equal(s.avgIntervalDays, 10)
})

test('입력 순서가 뒤죽박죽이어도 결과는 같다', () => {
  const s = customerStats([
    { date: '2026-01-11' },
    { date: '2026-01-21' },
    { date: '2026-01-01' },
  ])
  assert.equal(s.lastVisit, '2026-01-21')
  assert.equal(s.avgIntervalDays, 10)
})

test('평균 간격은 반올림한 정수', () => {
  // 1/1 → 1/08 → 1/09 : 총 8일 / 2번 = 4일
  const s = customerStats([
    { date: '2026-01-01' },
    { date: '2026-01-08' },
    { date: '2026-01-09' },
  ])
  assert.equal(s.avgIntervalDays, 4)
})
```

- [ ] **3-2. 테스트를 실행해 실패를 확인**

실행: `npm test`
예상: `Cannot find module '../js/stats.js'` 로 실패

- [ ] **3-3. `js/stats.js` 구현**

```js
// 하루를 밀리초로. 날짜 차이를 일수로 바꿀 때 쓴다.
const DAY_MS = 24 * 60 * 60 * 1000

// 'YYYY-MM-DD' 두 개 사이의 일수. 시간대 문제를 피하려고 UTC 정오로 고정해서 뺀다.
function daysBetween(fromDate, toDate) {
  const from = Date.parse(`${fromDate}T12:00:00Z`)
  const to = Date.parse(`${toDate}T12:00:00Z`)
  return Math.round((to - from) / DAY_MS)
}

// 한 손님의 방문 기록에서 요약을 뽑는다.
// visits: [{ date: 'YYYY-MM-DD', ... }] — 순서는 상관없다.
export function customerStats(visits) {
  if (visits.length === 0) {
    return { count: 0, lastVisit: null, avgIntervalDays: null }
  }

  // 날짜만 뽑아 오래된 순으로 정렬
  const dates = visits.map((v) => v.date).sort()
  const first = dates[0]
  const last = dates[dates.length - 1]

  // 방문이 한 번뿐이면 간격을 잴 수 없다
  const avgIntervalDays =
    dates.length < 2 ? null : Math.round(daysBetween(first, last) / (dates.length - 1))

  return { count: dates.length, lastVisit: last, avgIntervalDays }
}
```

- [ ] **3-4. 테스트를 실행해 통과를 확인**

실행: `npm test`
예상: 20개 테스트 모두 PASS

- [ ] **3-5. 커밋**

```bash
git add js/stats.js test/stats.test.js
git commit -m "손님 방문 이력 계산 추가 — 방문 횟수·마지막 방문·평균 간격"
```

---

## Task 4: 홈 화면 — 예약 목록 보기

**파일:**
- 생성: `index.html`
- 생성: `style.css`
- 생성: `js/dates.js`
- 생성: `js/home.js`

**인터페이스:**
- 사용: `createStore` (Task 2)
- 제공: `todayISO(date)` → `'YYYY-MM-DD'`, `formatDay(iso)` → `'3/17 (화)'` (Task 7에서도 쓴다)
- 제공: 브라우저에서 열리는 홈 화면

**확인 방법:** 이 작업부터는 자동 테스트가 아니라 **브라우저에서 눈으로 확인**한다. 화면 코드는 자동 테스트 비용이 실익보다 크다. 로직은 이미 Task 1~3에서 테스트했다.

---

- [ ] **4-1. `index.html` 만들기**

```html
<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
  <title>예약</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <header>
    <h1 id="monthTitle">예약</h1>
    <a class="icon-btn" href="settings.html" aria-label="설정">⚙</a>
  </header>

  <main id="list"></main>

  <button id="addBtn" class="fab">+ 직접 추가</button>

  <script type="module" src="js/home.js"></script>
</body>
</html>
```

- [ ] **4-2. `style.css` 만들기**

```css
/* 색은 최소한만 쓴다. 1단계에서 색이 들어가는 곳은 취소된 예약(회색)뿐이다.
   색이 적을수록 그 색이 나타났을 때 눈에 띈다.
   (2단계에서 "AI 판독이 확실하지 않은 항목"에 노란색이 하나 추가된다) */
:root {
  --bg: #ffffff;
  --text: #1a1a1a;
  --muted: #8a8a8a;
  --line: #e6e6e6;
  --accent: #1a1a1a;
}

* { box-sizing: border-box; }

body {
  margin: 0;
  padding: 0 0 88px;             /* 아래 버튼에 가리지 않게 여백 */
  font-family: -apple-system, BlinkMacSystemFont, "Apple SD Gothic Neo",
               "Noto Sans KR", sans-serif;
  font-size: 17px;               /* 폰에서 편하게 읽히는 크기 */
  color: var(--text);
  background: var(--bg);
}

header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 16px 20px;
  border-bottom: 1px solid var(--line);
  position: sticky;
  top: 0;
  background: var(--bg);
}

h1 { margin: 0; font-size: 20px; font-weight: 600; }

.icon-btn {
  font-size: 22px;
  text-decoration: none;
  color: var(--text);
  padding: 4px 8px;
}

main { padding: 8px 20px; }

/* 날짜 구분 줄 */
.day {
  margin: 24px 0 8px;
  font-size: 15px;
  color: var(--muted);
  font-weight: 600;
}

/* 예약 한 줄: 시각 / 이름 / 시술 */
.row {
  display: grid;
  grid-template-columns: 68px 1fr auto;
  gap: 12px;
  align-items: baseline;
  padding: 14px 0;
  border-bottom: 1px solid var(--line);
}

.row .time { font-variant-numeric: tabular-nums; color: var(--muted); }
.row .name { font-weight: 500; }
.row .name a { color: inherit; text-decoration: none; border-bottom: 1px solid var(--line); }
.row .service { color: var(--muted); font-size: 15px; }

/* 취소된 예약 */
.row.cancelled { color: var(--muted); }
.row.cancelled .time,
.row.cancelled .name,
.row.cancelled .service { text-decoration: line-through; }

.empty { color: var(--muted); padding: 40px 0; text-align: center; }

/* 아래 고정 버튼 */
.fab {
  position: fixed;
  left: 20px;
  right: 20px;
  bottom: 24px;
  padding: 16px;
  font-size: 17px;
  font-weight: 600;
  color: #fff;
  background: var(--accent);
  border: none;
  border-radius: 12px;
}

/* 폼 */
.field { margin: 20px 0; }
.field label { display: block; margin-bottom: 8px; font-size: 15px; color: var(--muted); }

input, select, textarea {
  width: 100%;
  padding: 14px;
  font-size: 17px;              /* 16px 미만이면 아이폰이 화면을 확대해버린다 */
  font-family: inherit;
  border: 1px solid var(--line);
  border-radius: 10px;
  background: var(--bg);
  color: var(--text);
}

.duration { display: flex; align-items: center; gap: 12px; }
.duration button {
  width: 52px; height: 52px;
  font-size: 22px;
  border: 1px solid var(--line);
  border-radius: 10px;
  background: var(--bg);
  color: var(--text);
}
.duration output { flex: 1; text-align: center; font-variant-numeric: tabular-nums; }
```

- [ ] **4-3. `js/dates.js` 만들기**

날짜 표시는 홈과 손님 이력 두 화면에서 똑같이 필요하다. 양쪽에 복사해두면 한쪽만 고치는 실수가 나므로 한 파일로 뺀다.

```js
const WEEKDAYS = ['일', '월', '화', '수', '목', '금', '토']

// 오늘 날짜를 'YYYY-MM-DD'로. 시간대 문제를 피하려고 직접 조립한다.
// (toISOString()을 쓰면 UTC 기준이라 한국 시간 오전 9시 이전에 날짜가 하루 밀린다)
export function todayISO(d = new Date()) {
  const pad = (n) => String(n).padStart(2, '0')
  return `${d.getFullYear()}-${pad(d.getMonth() + 1)}-${pad(d.getDate())}`
}

// 'YYYY-MM-DD' → '3/17 (화)'
export function formatDay(iso) {
  const [y, m, d] = iso.split('-').map(Number)
  const weekday = WEEKDAYS[new Date(y, m - 1, d).getDay()]
  return `${m}/${d} (${weekday})`
}
```

- [ ] **4-4. `js/home.js` 만들기 — 목록 표시만**

```js
import { createStore } from './store.js'
import { todayISO, formatDay } from './dates.js'

const store = createStore(localStorage)

// 오늘·내일에는 이름을 붙여 눈에 띄게 한다
function dayLabel(iso, today, tomorrow) {
  if (iso === today) return `오늘  ${formatDay(iso)}`
  if (iso === tomorrow) return `내일  ${formatDay(iso)}`
  return formatDay(iso)
}

// 예약 한 줄을 그린다
function renderRow(r) {
  const el = document.createElement('div')
  el.className = r.status === 'cancelled' ? 'row cancelled' : 'row'
  el.dataset.id = r.id

  const time = document.createElement('span')
  time.className = 'time'
  time.textContent = r.time

  const name = document.createElement('span')
  name.className = 'name'
  // 이름이 있으면 눌러서 이력으로 갈 수 있게 한다.
  // 언니가 종이에 이름을 안 적는 경우가 많으므로 없으면 그냥 비워둔다.
  if (r.customerName) {
    const link = document.createElement('a')
    link.href = `customer.html?name=${encodeURIComponent(r.customerName)}`
    link.textContent = r.customerName
    name.append(link)
  }

  const service = document.createElement('span')
  service.className = 'service'
  service.textContent = r.service

  el.append(time, name, service)
  return el
}

// 전체 목록을 다시 그린다.
// 오늘 이후만 보여준다 — 지나간 예약은 손님 이력 화면에서 본다.
function render() {
  const today = todayISO()
  const tomorrow = todayISO(new Date(Date.now() + 86400000))

  const upcoming = store
    .list()
    .filter((r) => r.date >= today)
    .sort((a, b) => a.date.localeCompare(b.date) || a.time.localeCompare(b.time))

  const list = document.getElementById('list')
  list.textContent = ''

  if (upcoming.length === 0) {
    const empty = document.createElement('p')
    empty.className = 'empty'
    empty.textContent = '예약이 없습니다'
    list.append(empty)
    return
  }

  // 날짜가 바뀔 때마다 구분 줄을 넣는다
  let currentDate = null
  for (const r of upcoming) {
    if (r.date !== currentDate) {
      currentDate = r.date
      const head = document.createElement('div')
      head.className = 'day'
      head.textContent = dayLabel(r.date, today, tomorrow)
      list.append(head)
    }
    list.append(renderRow(r))
  }
}

// 제목에 이번 달을 표시
const nowDate = new Date()
document.getElementById('monthTitle').textContent =
  `${nowDate.getMonth() + 1}월 ${nowDate.getFullYear()}`

render()
```

- [ ] **4-5. 브라우저에서 확인**

실행:
```bash
python3 -m http.server 8000
```
브라우저에서 `http://localhost:8000` 을 연다.

예상: "예약이 없습니다"가 보인다. 오른쪽 위 톱니바퀴와 아래 "+ 직접 추가" 버튼이 보인다(아직 눌러도 아무 일 없음).

개발자 도구 콘솔에서 아래를 실행해 가짜 예약을 넣고 새로고침한다.

```js
localStorage.setItem('reservations', JSON.stringify([
  { id:'a', date:'2026-03-17', time:'10:20', customerName:'화선언니',
    service:'염색', durationMin:90, status:'active', source:'manual',
    createdAt:'', updatedAt:'' },
  { id:'b', date:'2026-03-17', time:'14:00', customerName:'',
    service:'남자커트', durationMin:30, status:'active', source:'manual',
    createdAt:'', updatedAt:'' },
  { id:'c', date:'2026-03-18', time:'11:00', customerName:'김미영',
    service:'펌', durationMin:150, status:'cancelled', source:'manual',
    createdAt:'', updatedAt:'' },
]))
```

예상: 3/17 아래에 두 줄, 3/18 아래에 한 줄이 회색 취소선으로 보인다. "화선언니"에 밑줄이 있고 이름 없는 줄은 가운데가 비어 있다.

> 위 날짜가 이미 지났다면 아무것도 안 보인다(오늘 이후만 표시하므로). 그럴 때는 날짜를 오늘 이후로 바꿔서 넣는다.

- [ ] **4-6. 커밋**

```bash
git add index.html style.css js/dates.js js/home.js
git commit -m "홈 화면 추가 — 오늘 이후 예약을 날짜별로 표시"
```

---

## Task 5: 예약 직접 추가

**파일:**
- 수정: `index.html` (폼 추가)
- 수정: `js/home.js` (폼 동작 추가)

**인터페이스:**
- 사용: `SERVICES`, `defaultMinutes` (Task 1), `store.add` (Task 2)

**설계 메모:** 목표는 **3터치 입력**이다. 날짜·시각·소요 시간에 기본값이 미리 채워져 있어야 하고, 언니는 시술만 고르고 저장을 누르면 끝나야 한다. 언니는 종이에 소요 시간을 적지 않으므로, 그 칸에 손을 대게 만들면 안 된다.

---

- [ ] **5-1. `index.html`에 폼 추가**

`<button id="addBtn" ...>` 바로 앞에 아래를 넣는다.

```html
  <dialog id="addDialog">
    <form id="addForm" method="dialog">
      <div class="field">
        <label for="f-date">날짜</label>
        <input type="date" id="f-date" required>
      </div>

      <div class="field">
        <label for="f-time">시각</label>
        <input type="time" id="f-time" step="600" required>
      </div>

      <div class="field">
        <label for="f-service">시술</label>
        <select id="f-service"></select>
      </div>

      <div class="field">
        <label for="f-duration">예상 시간</label>
        <div class="duration">
          <button type="button" id="f-minus" aria-label="10분 줄이기">−</button>
          <output id="f-duration">90분</output>
          <button type="button" id="f-plus" aria-label="10분 늘리기">+</button>
        </div>
      </div>

      <div class="field">
        <label for="f-name">손님 이름 (선택)</label>
        <input type="text" id="f-name" autocomplete="off">
      </div>

      <div class="dialog-actions">
        <button type="button" id="f-cancel">닫기</button>
        <button type="submit" id="f-save">저장</button>
      </div>
    </form>
  </dialog>
```

- [ ] **5-2. `style.css`에 대화상자 스타일 추가**

파일 맨 아래에 붙인다.

```css
dialog {
  width: min(92vw, 420px);
  padding: 24px 20px;
  border: none;
  border-radius: 16px;
  background: var(--bg);
  color: var(--text);
}
dialog::backdrop { background: rgba(0, 0, 0, 0.4); }

.dialog-actions {
  display: flex;
  gap: 12px;
  margin-top: 28px;
}
.dialog-actions button {
  flex: 1;
  padding: 15px;
  font-size: 17px;
  font-family: inherit;
  border-radius: 10px;
  border: 1px solid var(--line);
  background: var(--bg);
  color: var(--text);
}
.dialog-actions button[type="submit"] {
  background: var(--accent);
  color: #fff;
  border-color: var(--accent);
  font-weight: 600;
}
```

- [ ] **5-3. `js/home.js`에 폼 동작 추가**

파일 맨 위 `import` 묶음에 한 줄을 **추가한다.** 기존 두 줄은 그대로 둔다 — Task 4의 `render()`와 `dayLabel()`이 `todayISO`/`formatDay`를 쓰고 있으므로 지우면 홈 화면이 깨진다.

```js
import { createStore } from './store.js'
import { todayISO, formatDay } from './dates.js'
import { SERVICES, defaultMinutes } from './services.js'   // ← 이 줄만 추가
```

그리고 파일 맨 아래(`render()` 호출 뒤)에 아래를 붙인다.

```js
// ── 예약 추가 ──────────────────────────────────────────

const dialog = document.getElementById('addDialog')
const serviceSelect = document.getElementById('f-service')
const durationOut = document.getElementById('f-duration')

// 시술 목록을 선택칸에 채운다
for (const s of SERVICES) {
  const opt = document.createElement('option')
  opt.value = s.name
  opt.textContent = s.name
  serviceSelect.append(opt)
}

// 현재 표시 중인 소요 시간(분)
let durationMin = defaultMinutes(SERVICES[0].name)

function showDuration() {
  durationOut.textContent = `${durationMin}분`
}

// 시술을 바꾸면 기본 시간이 자동으로 들어간다.
// 언니가 이 칸에 손을 안 대고도 저장할 수 있어야 한다.
serviceSelect.addEventListener('change', () => {
  durationMin = defaultMinutes(serviceSelect.value)
  showDuration()
})

// 10분 단위 조절. 같은 시술이라도 머리숱·기장에 따라 시간이 다르다.
document.getElementById('f-minus').addEventListener('click', () => {
  durationMin = Math.max(10, durationMin - 10)
  showDuration()
})
document.getElementById('f-plus').addEventListener('click', () => {
  durationMin += 10
  showDuration()
})

// 폼을 열 때마다 오늘 날짜와 다음 정시로 초기화한다
document.getElementById('addBtn').addEventListener('click', () => {
  const now = new Date()
  document.getElementById('f-date').value = todayISO(now)
  document.getElementById('f-time').value =
    `${String((now.getHours() + 1) % 24).padStart(2, '0')}:00`
  serviceSelect.selectedIndex = 0
  durationMin = defaultMinutes(SERVICES[0].name)
  showDuration()
  document.getElementById('f-name').value = ''
  dialog.showModal()
})

document.getElementById('f-cancel').addEventListener('click', () => dialog.close())

document.getElementById('addForm').addEventListener('submit', () => {
  store.add({
    date: document.getElementById('f-date').value,
    time: document.getElementById('f-time').value,
    service: serviceSelect.value,
    durationMin,
    customerName: document.getElementById('f-name').value.trim(),
  })
  render()
})
```

- [ ] **5-4. 브라우저에서 확인**

실행: `python3 -m http.server 8000` 후 `http://localhost:8000`

확인할 것:
1. "+ 직접 추가"를 누르면 폼이 뜨고 **날짜는 오늘, 시각은 다음 정시**가 미리 채워져 있다
2. 시술을 "펌"으로 바꾸면 예상 시간이 자동으로 **150분**이 된다
3. `+`를 두 번 누르면 **170분**이 된다
4. 이름을 비운 채 저장해도 목록에 들어간다
5. 저장하면 폼이 닫히고 목록이 즉시 갱신된다
6. 새로고침해도 예약이 남아 있다

- [ ] **5-5. 커밋**

```bash
git add index.html style.css js/home.js
git commit -m "예약 직접 추가 — 시술 선택 시 기본 시간 자동 입력, 10분 단위 조절"
```

---

## Task 6: 예약 취소와 삭제

**파일:**
- 수정: `js/home.js`

**인터페이스:**
- 사용: `store.cancel`, `store.remove` (Task 2)

**설계 메모:** 취소는 지우지 않고 회색 취소선으로 남긴다. 이 손님이 지난달에도 취소했는지 알 방법이 없어지면 안 되기 때문이다. 잘못 넣은 예약을 지우는 "삭제"는 별도로 둔다.

---

- [ ] **6-1. `js/home.js`에 줄 누르기 동작 추가**

파일 맨 아래에 붙인다.

```js
// ── 예약 취소 / 삭제 ────────────────────────────────────

// 예약 줄을 누르면 무엇을 할지 묻는다.
// 이름 링크를 누른 경우는 손님 이력으로 가야 하므로 여기서 처리하지 않는다.
document.getElementById('list').addEventListener('click', (e) => {
  if (e.target.closest('a')) return

  const row = e.target.closest('.row')
  if (!row) return

  const id = row.dataset.id
  const target = store.list().find((r) => r.id === id)
  if (!target) return

  const label = `${target.time} ${target.customerName || ''} ${target.service}`.trim()

  if (target.status === 'active') {
    if (confirm(`${label}\n\n취소로 표시할까요?`)) {
      store.cancel(id)
      render()
    }
    return
  }

  // 이미 취소된 예약을 다시 누르면 완전히 지울지 묻는다
  if (confirm(`${label}\n\n이 예약을 완전히 지울까요?`)) {
    store.remove(id)
    render()
  }
})
```

- [ ] **6-2. 브라우저에서 확인**

확인할 것:
1. 예약 줄을 누르면 "취소로 표시할까요?"가 뜬다
2. 확인하면 그 줄이 **회색 취소선**으로 바뀌고 목록에 남아 있다
3. 취소된 줄을 다시 누르면 "완전히 지울까요?"가 뜬다
4. 확인하면 목록에서 사라진다
5. **손님 이름을 누르면** 취소 확인창이 뜨지 않고 페이지 이동을 시도한다(아직 `customer.html`이 없으므로 404 — 정상)

- [ ] **6-3. 커밋**

```bash
git add js/home.js
git commit -m "예약 취소와 삭제 추가 — 취소는 취소선으로 남기고 삭제는 별도 확인"
```

---

## Task 7: 손님 이력 화면

**파일:**
- 생성: `customer.html`
- 생성: `js/customer.js`

**인터페이스:**
- 사용: `store.byCustomer` (Task 2), `customerStats` (Task 3), `formatDay` (Task 4)

**설계 메모:** 이 화면이 **앱의 존재 이유**다. 종이 달력은 지나가면 버려지지만 앱에는 남는다. 언니는 추가로 하는 일이 없고, 예약이 쌓이면 저절로 만들어진다.

---

- [ ] **7-1. `customer.html` 만들기**

```html
<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
  <title>손님</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <header>
    <h1><a class="back" href="index.html">←</a> <span id="name"></span></h1>
  </header>

  <main>
    <div id="visits"></div>
    <div id="summary" class="summary"></div>
  </main>

  <script type="module" src="js/customer.js"></script>
</body>
</html>
```

- [ ] **7-2. `style.css`에 이력 화면 스타일 추가**

파일 맨 아래에 붙인다.

```css
.back { text-decoration: none; color: var(--text); margin-right: 8px; }

/* 방문 기록 한 줄: 날짜 / 시술 */
.visit {
  display: flex;
  justify-content: space-between;
  padding: 14px 0;
  border-bottom: 1px solid var(--line);
}
.visit .date { font-variant-numeric: tabular-nums; }
.visit .service { color: var(--muted); }

.summary {
  margin-top: 28px;
  padding-top: 20px;
  border-top: 1px solid var(--line);
  color: var(--muted);
  line-height: 1.9;
}
```

- [ ] **7-3. `js/customer.js` 만들기**

```js
import { createStore } from './store.js'
import { customerStats } from './stats.js'
import { formatDay } from './dates.js'

const store = createStore(localStorage)

// 주소에서 손님 이름을 꺼낸다 (customer.html?name=화선언니)
const name = new URLSearchParams(location.search).get('name') || ''
document.getElementById('name').textContent = name

const visits = store.byCustomer(name)   // 최근 방문이 먼저
const visitsEl = document.getElementById('visits')

if (visits.length === 0) {
  const empty = document.createElement('p')
  empty.className = 'empty'
  empty.textContent = '방문 기록이 없습니다'
  visitsEl.append(empty)
} else {
  for (const v of visits) {
    const row = document.createElement('div')
    row.className = 'visit'

    const date = document.createElement('span')
    date.className = 'date'
    date.textContent = formatDay(v.date)

    const service = document.createElement('span')
    service.className = 'service'
    service.textContent = v.service

    row.append(date, service)
    visitsEl.append(row)
  }
}

// 요약 — 총 몇 번 왔고, 얼마 만에 한 번씩 오는지
const stats = customerStats(visits)
const lines = [`총 ${stats.count}회 방문`]
if (stats.avgIntervalDays !== null) {
  const weeks = Math.round(stats.avgIntervalDays / 7)
  lines.push(weeks >= 1 ? `평균 ${weeks}주마다 오심` : `평균 ${stats.avgIntervalDays}일마다 오심`)
}

const summaryEl = document.getElementById('summary')
for (const line of lines) {
  const p = document.createElement('div')
  p.textContent = line
  summaryEl.append(p)
}
```

- [ ] **7-4. 브라우저에서 확인**

홈에서 예약을 몇 개 넣되 **같은 이름으로 날짜를 다르게** 넣는다. 지난 날짜는 폼으로 넣을 수 있다.

확인할 것:
1. 홈에서 손님 이름을 누르면 이력 화면으로 간다
2. 방문 기록이 **최근순**으로 나온다
3. 아래에 "총 N회 방문"과 "평균 N주마다 오심"이 나온다
4. 한 번만 방문한 손님에게는 평균 줄이 안 나온다
5. `←`를 누르면 홈으로 돌아온다

- [ ] **7-5. 커밋**

```bash
git add customer.html style.css js/customer.js
git commit -m "손님 이력 화면 추가 — 방문 기록과 방문 주기 요약"
```

---

## Task 8: 설정과 데이터 내보내기

**파일:**
- 생성: `settings.html`
- 생성: `js/settings.js`

**인터페이스:**
- 사용: `store.exportJson` (Task 2)

**설계 메모:** 폰이 고장 나면 데이터가 전부 날아간다. 서버가 없으므로 **이 버튼이 유일한 안전장치**다. 언니가 파일을 자기 카톡으로 보내두면 백업이 된다.

AI 열쇠 입력칸은 2단계에서 이 화면에 추가한다. 지금은 만들지 않는다.

---

- [ ] **8-1. `settings.html` 만들기**

```html
<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
  <title>설정</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <header>
    <h1><a class="back" href="index.html">←</a> 설정</h1>
  </header>

  <main>
    <div class="field">
      <label>데이터 백업</label>
      <p class="hint">
        폰이 고장 나면 예약 기록이 사라집니다.
        가끔 내려받아 자기 카톡으로 보내두세요.
      </p>
      <button id="exportBtn" class="wide">파일로 내려받기</button>
      <p class="hint" id="count"></p>
    </div>
  </main>

  <script type="module" src="js/settings.js"></script>
</body>
</html>
```

- [ ] **8-2. `style.css`에 설정 화면 스타일 추가**

파일 맨 아래에 붙인다.

```css
.hint { color: var(--muted); font-size: 15px; line-height: 1.7; margin: 8px 0 16px; }

button.wide {
  width: 100%;
  padding: 16px;
  font-size: 17px;
  font-family: inherit;
  border: 1px solid var(--line);
  border-radius: 10px;
  background: var(--bg);
  color: var(--text);
}
```

- [ ] **8-3. `js/settings.js` 만들기**

```js
import { createStore } from './store.js'

const store = createStore(localStorage)

// 저장된 예약이 몇 건인지 보여준다 — 백업이 비어 있지 않은지 눈으로 확인할 수 있게
document.getElementById('count').textContent = `현재 ${store.list().length}건이 저장되어 있습니다.`

// 예약 전체를 JSON 파일로 내려받는다.
// 브라우저에 임시 주소를 만들어 가짜 링크를 누르는 방식 — 서버 없이 파일을 만드는 표준 방법이다.
document.getElementById('exportBtn').addEventListener('click', () => {
  const blob = new Blob([store.exportJson()], { type: 'application/json' })
  const url = URL.createObjectURL(blob)

  const today = new Date().toISOString().slice(0, 10)
  const a = document.createElement('a')
  a.href = url
  a.download = `예약백업-${today}.json`
  a.click()

  URL.revokeObjectURL(url)   // 다 쓴 임시 주소는 바로 정리한다
})
```

- [ ] **8-4. 브라우저에서 확인**

확인할 것:
1. 홈 오른쪽 위 톱니바퀴를 누르면 설정으로 간다
2. "현재 N건이 저장되어 있습니다"의 숫자가 실제 예약 수와 같다
3. "파일로 내려받기"를 누르면 `예약백업-2026-07-28.json` 이 받아진다
4. 그 파일을 열면 예약 내용이 사람이 읽을 수 있는 형태로 들어 있다

- [ ] **8-5. 커밋**

```bash
git add settings.html style.css js/settings.js
git commit -m "설정 화면과 데이터 내보내기 추가"
```

---

## Task 9: 폰에 설치 가능하게 만들기 (PWA)

**파일:**
- 생성: `manifest.json`
- 생성: `sw.js`
- 생성: `icon-192.png`, `icon-512.png`
- 수정: `index.html`, `customer.html`, `settings.html` (manifest 연결)
- 수정: `js/home.js` (서비스 워커 등록)

**설계 메모:** 이걸 붙여야 언니가 홈 화면에 아이콘으로 두고 일반 앱처럼 쓸 수 있다. 갤럭시에서는 앱 서랍에도 들어가고 주소창 없이 전체화면으로 뜬다. 서비스 워커는 파일을 폰에 저장해두어 **인터넷이 끊겨도 앱이 열리게** 한다.

---

- [ ] **9-1. 아이콘 만들기**

```bash
python3 -c "
from PIL import Image, ImageDraw
# 검은 바탕에 흰 원 하나. 나중에 마음에 드는 아이콘으로 바꾸면 된다.
for size in (192, 512):
    img = Image.new('RGB', (size, size), '#1a1a1a')
    d = ImageDraw.Draw(img)
    pad = size // 4
    d.ellipse([pad, pad, size - pad, size - pad], outline='#ffffff', width=size // 24)
    img.save(f'icon-{size}.png')
print('아이콘 생성 완료')
"
```

- [ ] **9-2. `manifest.json` 만들기**

```json
{
  "name": "예약",
  "short_name": "예약",
  "start_url": "index.html",
  "display": "standalone",
  "background_color": "#ffffff",
  "theme_color": "#1a1a1a",
  "lang": "ko",
  "icons": [
    { "src": "icon-192.png", "sizes": "192x192", "type": "image/png" },
    { "src": "icon-512.png", "sizes": "512x512", "type": "image/png" }
  ]
}
```

- [ ] **9-3. `sw.js` 만들기**

```js
// 앱 파일을 폰에 저장해두어 인터넷이 끊겨도 열리게 한다.
// 파일을 고칠 때마다 CACHE 이름의 숫자를 올려야 새 버전이 반영된다.
const CACHE = 'reservation-v1'

const FILES = [
  'index.html', 'customer.html', 'settings.html', 'style.css',
  'js/services.js', 'js/store.js', 'js/stats.js', 'js/dates.js',
  'js/home.js', 'js/customer.js', 'js/settings.js',
  'manifest.json', 'icon-192.png', 'icon-512.png',
]

self.addEventListener('install', (e) => {
  e.waitUntil(caches.open(CACHE).then((c) => c.addAll(FILES)))
})

// 이름이 다른 옛 캐시는 지운다
self.addEventListener('activate', (e) => {
  e.waitUntil(
    caches.keys().then((keys) =>
      Promise.all(keys.filter((k) => k !== CACHE).map((k) => caches.delete(k)))
    )
  )
})

// 저장해둔 파일이 있으면 그걸 주고, 없으면 인터넷에서 가져온다
self.addEventListener('fetch', (e) => {
  e.respondWith(caches.match(e.request).then((hit) => hit || fetch(e.request)))
})
```

- [ ] **9-4. 세 HTML 파일의 `<head>`에 manifest 연결**

`index.html`, `customer.html`, `settings.html` 각각의 `<link rel="stylesheet" ...>` 아래에 같은 두 줄을 넣는다.

```html
  <link rel="manifest" href="manifest.json">
  <meta name="theme-color" content="#1a1a1a">
```

- [ ] **9-5. `js/home.js` 맨 아래에 서비스 워커 등록 추가**

```js
// 서비스 워커 등록 — 오프라인 동작과 홈 화면 설치를 가능하게 한다
if ('serviceWorker' in navigator) {
  navigator.serviceWorker.register('sw.js')
}
```

- [ ] **9-6. 확인**

실행: `python3 -m http.server 8000` 후 `http://localhost:8000`

확인할 것:
1. 개발자 도구 → Application → Manifest 에 "예약"과 아이콘이 보인다
2. Application → Service Workers 에 `sw.js`가 activated 상태다
3. 개발자 도구에서 Offline을 켜고 새로고침해도 앱이 열린다

- [ ] **9-7. 전체 테스트를 다시 실행**

실행: `npm test`
예상: 20개 테스트 모두 PASS (화면 작업이 로직을 깨지 않았는지 확인)

- [ ] **9-8. 커밋**

```bash
git add manifest.json sw.js icon-192.png icon-512.png index.html customer.html settings.html js/home.js
git commit -m "PWA 설정 추가 — 홈 화면 설치와 오프라인 동작"
```

---

## Task 10: 배포와 폰 설치

**파일:** 없음 (설정 작업)

**설계 메모:** 정적 파일뿐이라 어디에 올려도 된다. 무료이고 `git push`만 하면 자동 반영되는 곳을 쓴다.

---

- [ ] **10-1. GitHub 저장소 만들기**

```bash
gh repo create reservation --private --source=. --push
```

`gh`가 없으면 github.com에서 private 저장소를 만들고 아래를 실행한다.

```bash
git remote add origin https://github.com/persian32/reservation.git
git branch -M main
git push -u origin main
```

**private으로 만든다.** 손님 정보를 다루는 앱이다.

- [ ] **10-2. GitHub Pages 켜기**

저장소 → Settings → Pages → Source를 `Deploy from a branch`, Branch를 `main` / `/ (root)` 로 두고 저장한다.

몇 분 뒤 `https://persian32.github.io/reservation/` 에서 열린다.

> Pages는 저장소가 private이면 유료 플랜에서만 동작한다. 무료 계정이라면 저장소를 public으로 바꾸는 대신 **Vercel이나 Netlify에 GitHub 계정으로 로그인해 이 저장소를 연결**한다. 둘 다 private 저장소를 무료로 배포해준다. 코드에 비밀값이 하나도 없으므로(열쇠는 사용자가 각자 입력) 어느 쪽이든 안전하다.

- [ ] **10-3. 본인 아이폰에 설치**

사파리로 배포 주소를 연다 → 공유 버튼 → **홈 화면에 추가**

확인할 것: 아이콘이 생기고, 눌렀을 때 주소창 없이 전체화면으로 뜬다.

- [ ] **10-4. 며칠 직접 써본다**

언니에게 주기 전에 본인이 먼저 쓴다. 예약을 몇 개 넣어보고, 취소해보고, 이력 화면을 열어본다.

**여기서 잡아야 할 것:** 입력이 몇 초 걸리는지, 손가락으로 누르기 불편한 곳은 없는지, 폰에서 글씨가 작지 않은지.

- [ ] **10-5. README 쓰고 커밋**

`README.md`:

```markdown
# 예약

미용실 예약 관리 웹앱. 서버 없이 폰 안에서만 동작한다.

## 실행

```bash
python3 -m http.server 8000
```

http://localhost:8000

## 테스트

```bash
npm test
```

## 구조

- `js/services.js` 시술 목록과 기본 소요 시간
- `js/store.js` 예약 저장·조회 (localStorage)
- `js/stats.js` 손님 방문 이력 계산
- `js/dates.js` 날짜 표시 형식 변환
- `js/home.js` / `customer.js` / `settings.js` 각 화면 동작

앞의 세 파일은 순수 로직이고 `test/`에서 테스트한다. 뒤의 세 파일은 DOM만 다룬다.

## 데이터

브라우저 `localStorage`의 `reservations` 키에 JSON 배열로 저장된다.
서버·계정·네트워크 요청이 없다. 설정 화면에서 파일로 내보낼 수 있다.

## 설계 문서

`docs/specs/2026-07-28-헤어살롱-예약앱-설계.md`
```

```bash
git add README.md
git commit -m "README 추가"
git push
```

---

## 완료 기준

1단계가 끝났다고 말하려면 아래가 전부 참이어야 한다.

- [ ] `npm test` 20개 테스트 통과
- [ ] 배포된 주소에서 앱이 열린다
- [ ] 본인 아이폰 홈 화면에 설치되어 전체화면으로 뜬다
- [ ] 예약을 추가하고 새로고침해도 남아 있다
- [ ] 손님 이름을 누르면 방문 이력과 평균 주기가 나온다
- [ ] 예약을 취소하면 회색 취소선으로 남는다
- [ ] 설정에서 백업 파일이 받아진다
- [ ] 비행기 모드에서도 앱이 열린다
- [ ] 코드에 API 열쇠·비밀번호·유료 서비스 호출이 하나도 없다

## 1단계 이후

- **0단계 검증** — 언니의 실제 달력 사진으로 AI 판독이 되는지 확인. 이 결과에 따라 2단계를 할지 말지 정한다.
- **2단계 계획서** — 0단계가 성공한 뒤에 따로 쓴다. 설정에 AI 열쇠 칸을 추가하고, 열쇠가 있으면 카메라 버튼과 확인 화면이 나타나는 구조.
- **언니에게 물어볼 것** — 설계 문서 10장 참고 (여자커트·염색·매직·클리닉 실제 소요 시간, 시술 중복 여부, 달력 수정 방식)
